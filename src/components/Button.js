import React from 'react';
import RaisedButton from 'material-ui/RaisedButton';
import PropTypes from 'prop-types';

const Button = () => <RaisedButton label="Search" primary />;

Button.propTypes = {};

export default Button;
